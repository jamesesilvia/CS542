#delete database
#note that while a server is active, a database.dat file exists and the data is stored internally
#therefore this should be run while the server is not running
rm ../build/database.txt